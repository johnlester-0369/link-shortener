
# Firebase Service Account Key (base64 encoded JSON)
# Generate from Firebase Console > Project Settings > Service Accounts > Generate New Private Key
# Then convert to base64: base64 -i serviceAccountKey.json
FIREBASE_SERVICE_ACCOUNT_KEY=your_base64_encoded_service_account_key_here

# Link Shortener URL prefix (domain where shortened links will be hosted)
# Example: https://lnk.sh or https://yourdomain.com
# Used to construct full shortened URLs returned to clients
LINK_SHORTENER_URL=http://localhost:3000
